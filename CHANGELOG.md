# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)，所有值得记录的变更都会汇总在此文件。
版本格式：`v<主>.<次>.<修订>`，例如 `v1.2.1`。

## [1.3.2] - 2026-09-12

### 变更

- **仪表盘卡片示例（`dashboard/qingdao_taineng_gas_cards.yaml`）结构调整**：
  - 计费账期信息并入「用量概览」标题的副标题，直接显示
    `账期：<cycle_start> → <cycle_end>（非自然月，抄表日结算）`，
    删除原来独立的账期 markdown 卡片，卡片总数由 13 张精简为 12 张；
  - 「阶梯气价剩余」三档卡片的副标题精简为纯单价（`3.54 / 4.12 / 4.99 元/m³`），
    档位改由 `mdi:numeric-1/2/3-circle-outline` 图标区分；
  - 「累计表读数」副标题统一为「抄表数 · 抄表日期」措辞。
- **修正卡片样式笔误**：第三档（4.99 元/m³）卡片的 `--card-secondary-font-size: px`
  缺少数值（该条 CSS 声明会整条失效），已补为 `10px`，与一、二档保持一致。
- **示例卡片全面脱敏**：实体 ID 统一为 `ty_1234` 占位前缀，标题栏地址改为示例值
  （`示例小区 1#1-101`），文件头部注释补充了实体替换说明与隐私提示，
  并说明标题栏既可用固定地址、也可改回读取 `user_no` 属性。
- README「仪表盘卡片（示例）」章节的卡片结构说明同步更新。

### 升级说明

- 通过 HACS 更新到 `v1.3.2` 后，**需要把 `dashboard/qingdao_taineng_gas_cards.yaml`
  的内容重新复制一次到仪表盘**（仪表盘中已有的旧卡片不会自动更新），
  并把其中的实体 ID 换成自己的户号前缀。
- 本次仅涉及仪表盘示例文件与文档，**集成代码无任何改动**，升级后无需重新加载集成，
  也不会影响已采集的历史数据。

## [1.3.1] - 2026-09-11

### 修复

- **仪表盘卡片的两张趋势图无法正常显示数据**：
  - 两张 `apexcharts-card` 都没有设置 `graph_span`，而默认显示窗口只有 **24 小时**，
    导致「最近 14 天用气量」的 14 个数据点几乎全部落在窗口之外——表现为 x 轴退化成
    `16:00~13:00` 的时刻轴、且只剩一根柱；「月度用气趋势」因窗口内一个数据点都没有，
    一直显示 `Loading...`。现在分别设置 `graph_span: 15d`（配合 `span: end: day`，锁定为
    「今天往前 14 天」）与 `graph_span: 12mo`（配合 `span: start: year`，全年视图）。
  - 日期解析改为按「年 / 月 / 日」构造本地时间（`new Date(y, m-1, d)`），
    不再使用 `new Date('2026-09-10')`（会被按 UTC 解析，造成日期/时刻偏移）；
    月度 `month` 字段兼容 `2026-08`、`2026-08-31`、`2026年8月` 等格式，
    非 `YYYY-MM` 格式不再产生 `Invalid Date` 导致空图。
  - x 轴标签显式指定为「月-日」与「M月」，不再显示为时刻；两张图均新增
    `show.loading: false`，无数据时不再卡在 `Loading...`。
- **卡片整体背景色不生效**：`vertical-stack` 内部**没有 `ha-card` 元素**，
  原外层样式选择器写成 `ha-card` 时匹配不到任何元素。现改用 `:host` / `#root`，
  并可通过 CSS 变量 `--tn-bg-card` 统一调整整体底色。

### 已知限制

- 月度趋势只包含 **当年已出账月份**，月初当月数据可能尚未出账（此时该图为空白，属正常）；
- 卡片中的自然月累计与「本账期累计用量」口径不同（泰能按抄表日为非自然月计费），
  卡片已用账期小注明确区分，请勿混用。

### 升级说明

- 通过 HACS 更新到 `v1.3.1` 后，**需要把 `dashboard/qingdao_taineng_gas_cards.yaml`
  的内容重新复制一次到仪表盘**（仪表盘中已有的旧卡片不会自动更新），
  并把其中的实体 ID 换成自己的户号前缀。

## [1.3.0] - 2026-09-11

### 新增

- **仪表盘卡片示例**：新增 `dashboard/qingdao_taineng_gas_cards.yaml`，
  仿国网电费卡片风格（Nord 深色玻璃拟态），提供主卡片、信息卡、阶梯气价剩余与
  两张趋势图，并在 README 增加「仪表盘卡片（示例）」章节。
  - 仅依赖 `mushroom` + `apexcharts-card` + `card-mod`，**未使用** `stack-in-card`；
  - 容器改用 HA 内置 `vertical-stack` / `grid`，无需额外安装卡片；
  - 所有数值取自本集成实体的状态与属性，不需要自建模板传感器。
- **`最近一日用量` 的 `recent_days` 属性由 7 天扩展为 14 天**，用于卡片绘制 14 天趋势图。
- **`当年累计用量` 新增 `monthly` 属性**：当年逐月用量明细
  `[{month, volume}, ...]`，数据来自 `fee/chart/iotBarChart` 的 `type=year` 汇总，
  供卡片绘制「当年已出账各月趋势」柱状图。
  - 月用量接口失败不影响日用量主流程（单独捕获，仅记录警告日志）。

### 已知限制

- 月度趋势只包含 **当年已出账月份**，月初当月数据可能尚未出账；
- 卡片中的自然月累计与「本账期累计用量」口径不同（泰能按抄表日为非自然月计费），
  卡片已用账期小注明确区分，请勿混用。

## [1.2.3] - 2026-09-09

### 修复

- 修复每日查询计划到点后不执行、日志出现
  `RuntimeWarning: coroutine 'TanengGasCoordinator._run_scheduled_query' was never awaited` 的问题。
  - 原因：定时器回调 `_fire_query_timer` 是普通同步方法，HA 事件系统会把此类回调
    （非 `async def`、也非 `@callback`）当作 Executor job 放进**线程池**执行；
    在线程里调用 `async_create_task` 无法把协程送入事件循环，导致计划中的刷新从未运行，
    数据停留在上一次成功查询。
  - 修复：把回调改为协程函数（`async def`），让 HA 在事件循环内直接调度并 `await`
    刷新，查询计划恢复正常，同时消除了该 RuntimeWarning。
- 修复后，从 1.2.2 升级的用户需要在 HA 中**更新/重载本集成一次**（重载后查询计划
  即按新代码重排）；若升级时当天计划时刻已过，可在集成「重新配置/选项」里触发一次
  手动更新，或等待下一个计划时刻自动补查。

## [1.2.2] - 2026-09-07

### 新增

- **可配置的每日查询计划**（集成选项）：
  - 新增「每天查询次数」，范围 1–4（默认 4），限制每天对平台的自动查询次数；
  - 新增「基础查询时间」（默认 `06:00`），其余查询时刻按 24 小时均分自动推导
    （间隔 = 24h ÷ 次数），例如 4 次 + `06:00` → `00:00 / 06:00 / 12:00 / 18:00`；
  - 每次真实查询都会写入 HA 日志：当日第几次、耗时、数据截至日期、成功/失败
    原因及下一次查询时刻；集成加载/卸载、选项变更后查询计划自动重建。
- 原「每 6 小时轮询一次（与加载时刻相关）」改为固定时钟的每日查询计划，
  行为更可预期，且默认频率（4 次/天）保持不变。

### 已知限制

- 查询计划按 HA 本地时区的墙钟时刻执行，重启/改选项后会立即按新计划重排；
- 燃气平台数据每日才更新一次，多次查询主要用于尽早拿到当天数据，
  不代表数据每小时都在变；请勿把次数调大后取消该限制（仍受 1–4 限制约束）。

## [1.2.1] - 2026-09-07

### 修复

- 修复「每日耗气量 YYYY-MM-DD」实体在 HA 日志中反复出现
  `state class 'measurement' which is impossible considering device class ('gas')` 警告的问题。
  HA 校验要求 `device_class=gas` 的传感器 `state_class` 只能为
  `None / total / total_increasing`，不允许 `measurement`。
  由于该系列实体是「单个已结算日的耗气量快照」，创建后值固定、不随时间变化，
  既非测量值也非累计量，因此移除了错误的 `state_class=measurement`（保持默认 None），
  仅保留 `device_class: gas` 用于展示，警告随之消除。

### 已知限制

- 「每日耗气量」实体不参与 HA 长期统计与能源面板（值固定且非累计口径），
  逐日核对 / 画图请直接使用其实体状态。
